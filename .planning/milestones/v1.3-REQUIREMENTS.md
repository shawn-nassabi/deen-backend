# Requirements Archive: v1.3 Sentry Deep Integration

**Archived:** 2026-04-28
**Milestone goal:** Instrument all main API endpoints with structured info/warning/error logging that flows to Sentry with correlation_id per request — production-only, opt-in via env var.
**Scope:** Pure observability pass — no API surface changes, no LangGraph topology changes, no new database tables.
**Outcome:** All 22 requirements delivered across 4 phases (8 plans).

---

## Requirements (All Complete)

### INFRA — Sentry Configuration & Infrastructure

- [x] **INFRA-01**: System sends zero data to Sentry when `SENTRY_ENABLED` is `false` or unset — local dev never triggers Sentry events
  - *Delivered: Phase 13 (13-01, 13-02) — SENTRY_ENABLED + SENTRY_DSN dual-gate in core/sentry.py*
- [x] **INFRA-02**: `main.py` initializes Sentry only when both `SENTRY_ENABLED=true` AND `SENTRY_DSN` are set; `LoggingIntegration(level=INFO, event_level=ERROR, sentry_logs_level=INFO)` is explicitly configured in `sentry_sdk.init()`
  - *Delivered: Phase 13 (13-01, 13-02) — side-effect import core.sentry; sentry_sdk.init() moved to core/sentry.py*
- [x] **INFRA-03**: Every HTTP request carries a unique `correlation_id` UUID; all log events from that request include it — enabling full request-chain filtering in Sentry
  - *Delivered: Phase 13 (13-01, 13-02) — CorrelationIdMiddleware + correlation_id ContextVar*
- [x] **INFRA-04**: Sentry events for a request include `session_id`, `user_id` (when authenticated), and `endpoint` as searchable tags on the per-request isolation scope
  - *Delivered: Phase 13 (13-01) — bind_sentry_scope() in core/sentry.py; wired in Phases 14–16*
- [x] **INFRA-05**: `send_default_pii=True` removed; a `before_send` hook redacts `user_query` and request body content from Sentry events (GDPR Article 9 compliance — Islamic religious content is special-category data)
  - *Delivered: Phase 13 (13-01) — _scrub_pii() hook removes event["request"]["data"]*

### CHAT — Agentic Chat API (`api/chat.py`)

- [x] **CHAT-01**: `/chat/stream/agentic` and `/chat/agentic` log request start and completion at INFO with `correlation_id`, `session_id`, and `endpoint` in structured `extra={}`
  - *Delivered: Phase 14 (14-03) — start + completion INFO logs in both agentic handlers*
- [x] **CHAT-02**: Config parse errors logged at WARNING; unhandled exceptions logged at ERROR and captured in Sentry — no duplicate events (pick `logger.error(exc_info=True)` OR `capture_exception()`, not both)
  - *Delivered: Phase 14 (14-03) — config parse at WARNING; logger.error(exc_info=True) only*
- [x] **CHAT-03**: All `print()` calls in `api/chat.py` replaced with `logger.*`
  - *Delivered: Phase 14 (14-03) — 0 print() calls remaining; import traceback removed*

### REF — References API (`api/reference.py`)

- [x] **REF-01**: `/references` logs request start and completion at INFO; exceptions logged at ERROR with `correlation_id` context
  - *Delivered: Phase 14 (14-01) — start + completion INFO logs added*
- [x] **REF-02**: HTTP 500 response body no longer exposes raw exception string (data-leak bug fixed — exception string was previously returned as `detail`)
  - *Delivered: Phase 14 (14-01) — detail="internal_error" replaces detail=f"...{str(e)}"*
- [x] **REF-03**: All `print()` calls in `api/reference.py` replaced with `logger.*`
  - *Delivered: Phase 14 (14-01) — 0 print() calls remaining*

### HIK — Hikmah API (`api/hikmah.py`)

- [x] **HIK-01**: All existing `logger.*` calls in `api/hikmah.py` include `correlation_id` in `extra={}`
  - *Delivered: Phase 14 (14-01) — correlation_id injected into all 7 quiz CRUD handler log calls*
- [x] **HIK-02**: Remaining `print()` call (in `/hikmah/elaborate/stream` error path) replaced with `logger.*`
  - *Delivered: Phase 14 (14-01) — print()+traceback.print_exc() replaced with logger.error(exc_info=True)*

### PRIM — Primers API (`api/primers.py`)

- [x] **PRIM-01**: All `logger.*` calls in `api/primers.py` converted from f-string interpolation to structured `extra={}` so fields like `lesson_id` and `user_id` are searchable in Sentry Logs
  - *Delivered: Phase 14 (14-02) — all 3 handlers converted; bind_sentry_scope() added*

### PIPE — Core Pipeline (`core/pipeline_langgraph.py`)

- [x] **PIPE-01**: All `print()` calls replaced with `logger.*`; LangGraph per-node traversal events logged at DEBUG (not INFO — avoids Sentry log quota overrun at scale)
  - *Delivered: Phase 15 (15-01) — 5 print() → logger.*; node traversal at logger.debug()*
- [x] **PIPE-02**: Pipeline start/end and SSE generator exceptions captured correctly with no duplicate Sentry events; `correlation_id` included in all pipeline log calls
  - *Delivered: Phase 15 (15-01) — logger.error(exc_info=True) only; no capture_exception()*

### TOOL — Agent Tools (`agents/tools/retrieval_tools.py`, `agents/core/chat_agent.py`)

- [x] **TOOL-01**: All `print()` calls in `agents/tools/retrieval_tools.py` replaced with `logger.error()` including query snippet and exception context in `extra={}`
  - *Delivered: Phase 15 (15-01) — 4 print() → logger.error() with correlation_id in extra={}*
- [x] **TOOL-02**: Any `print()` calls in `agents/core/chat_agent.py` replaced with `logger.*`
  - *Delivered: Phase 15 (15-02) — ~20 print() sites replaced; 25 logger.debug() + 7 logger.error()*

### FIQH — Fiqh Sub-graph (`agents/fiqh/fiqh_graph.py`)

- [x] **FIQH-01**: All existing log calls converted from `%s` format strings to `extra={}` with `iteration`, `verdict`, and `doc_count` as top-level searchable fields in Sentry Logs
  - *Delivered: Phase 16 (16-01) — 10 log calls converted to extra={} style*
- [x] **FIQH-02**: WARNING logged when zero documents are retrieved on any FAIR-RAG iteration
  - *Delivered: Phase 16 (16-01) — logger.warning() in _retrieve_node when retrieved_docs is empty*
- [x] **FIQH-03**: WARNING logged when the evidence filter removes all accumulated documents (fail-open path triggered)
  - *Delivered: Phase 16 (16-01) — logger.warning() before fail-open in _filter_node*
- [x] **FIQH-04**: WARNING logged when max iterations are reached with an INSUFFICIENT evidence verdict
  - *Delivered: Phase 16 (16-01) — logger.warning() guarded by verdict != "SUFFICIENT" in _assess_node*

---

## Traceability

| REQ-ID | Phase | Plan | Status |
|--------|-------|------|--------|
| INFRA-01 | Phase 13 | 13-01, 13-02 | ✅ Complete |
| INFRA-02 | Phase 13 | 13-01, 13-02 | ✅ Complete |
| INFRA-03 | Phase 13 | 13-01, 13-02 | ✅ Complete |
| INFRA-04 | Phase 13 | 13-01 | ✅ Complete |
| INFRA-05 | Phase 13 | 13-01 | ✅ Complete |
| CHAT-01 | Phase 14 | 14-03 | ✅ Complete |
| CHAT-02 | Phase 14 | 14-03 | ✅ Complete |
| CHAT-03 | Phase 14 | 14-03 | ✅ Complete |
| REF-01 | Phase 14 | 14-01 | ✅ Complete |
| REF-02 | Phase 14 | 14-01 | ✅ Complete |
| REF-03 | Phase 14 | 14-01 | ✅ Complete |
| HIK-01 | Phase 14 | 14-01 | ✅ Complete |
| HIK-02 | Phase 14 | 14-01 | ✅ Complete |
| PRIM-01 | Phase 14 | 14-02 | ✅ Complete |
| PIPE-01 | Phase 15 | 15-01 | ✅ Complete |
| PIPE-02 | Phase 15 | 15-01 | ✅ Complete |
| TOOL-01 | Phase 15 | 15-01 | ✅ Complete |
| TOOL-02 | Phase 15 | 15-02 | ✅ Complete |
| FIQH-01 | Phase 16 | 16-01 | ✅ Complete |
| FIQH-02 | Phase 16 | 16-01 | ✅ Complete |
| FIQH-03 | Phase 16 | 16-01 | ✅ Complete |
| FIQH-04 | Phase 16 | 16-01 | ✅ Complete |

**22/22 requirements complete.**

---

## Deferred to Future Milestone

- Sentry Performance tracing — custom spans for retrieval, LLM generation, fiqh iterations
- `before_send_log` hook for fine-grained Sentry log quota throttling (stable at sentry-sdk >= 2.35.0; current pin is 2.27.0)
- LLM token counts in log context (input/output tokens per Claude call)
- `ExtraFormatter` ANSI colorization disabled for non-development environments
