---
phase: 18-module-prompt-restructuring
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - tests/test_agentic_streaming_pipeline.py
autonomous: false
requirements:
  - STRUCT-02

must_haves:
  truths:
    - "tests/test_agentic_streaming_pipeline.py monkeypatches core.prompt_templates.generator_messages (not generator_prompt_template)"
    - "fake_model_fn receives chat_history list directly (not a dict payload with chat_history key)"
    - "pytest -q passes with zero failures"
    - "grep for ChatPromptTemplate.from_messages across modules/fiqh/, modules/classification/, modules/translation/, modules/generation/, core/pipeline_langgraph.py returns 0 results"
    - "grep for ChatPromptTemplate in all refactored modules returns 0 results"
  artifacts:
    - path: "tests/test_agentic_streaming_pipeline.py"
      provides: "updated monkeypatch for generator_messages builder function"
      contains: "generator_messages"
  key_links:
    - from: "tests/test_agentic_streaming_pipeline.py"
      to: "core/prompt_templates.generator_messages"
      via: "monkeypatch.setattr('core.prompt_templates.generator_messages', lambda ...)"
      pattern: "generator_messages"
---

<objective>
Update the test monkeypatch that references the now-deleted generator_prompt_template object, then run the full test suite to confirm zero behavioral regression from the Phase 18 refactor.

Purpose: Phase 18 success criterion 4 requires a smoke test confirming zero regression. The test at tests/test_agentic_streaming_pipeline.py:185 currently patches core.prompt_templates.generator_prompt_template (which no longer exists after Plan 01). It must patch generator_messages instead. The fake_model_fn must also be updated — after the refactor, pipeline_langgraph.py calls chat_model.stream(messages_list) directly, so the mock receives a messages list rather than a dict.

Output:
- tests/test_agentic_streaming_pipeline.py: monkeypatch and fake_model_fn updated
- Checkpoint: pytest -q passes, /chat/stream/agentic smoke test confirmed
</objective>

<execution_context>
@/Users/shawn.n/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawn.n/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-module-prompt-restructuring/18-CONTEXT.md
@.planning/phases/18-module-prompt-restructuring/18-RESEARCH.md
@.planning/phases/18-module-prompt-restructuring/18-PATTERNS.md
@.planning/phases/18-module-prompt-restructuring/18-01-SUMMARY.md
@.planning/phases/18-module-prompt-restructuring/18-02-SUMMARY.md

<interfaces>
<!-- generator_messages signature (from Plan 01) -->
def generator_messages(
    query: str,
    references: str,
    target_language: str = "english",
    chat_history: list | None = None,
) -> list

<!-- pipeline_langgraph.py call pattern after Plan 02 -->
messages = prompt_templates.generator_messages(
    query=user_query,
    references=references,
    target_language=target_language,
    chat_history=history_messages,  # list of BaseMessage from make_history()
)
for chunk in chat_model.stream(messages): ...
# The monkeypatched chat_model receives the messages list directly

<!-- Test pattern from RESEARCH.md Pattern 11 -->
monkeypatch.setattr(
    "core.prompt_templates.generator_messages",
    lambda query, references, target_language, chat_history: chat_history,
)
# lambda returns chat_history so fake_model_fn receives it as payload
# assertion: captured["history"][0].content == "Earlier context" still works
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test monkeypatch and fake_model_fn in test_agentic_streaming_pipeline.py</name>
  <read_first>
    - tests/test_agentic_streaming_pipeline.py (read entire file — understand test setup, FakeHistory, fake_model_fn at lines ~176-178, monkeypatch at line ~185, assertion at line ~206)
    - .planning/phases/18-module-prompt-restructuring/18-PATTERNS.md (Pattern 11 section — exact before/after for test monkeypatch and fake_model_fn)
  </read_first>
  <files>tests/test_agentic_streaming_pipeline.py</files>
  <action>
Read the entire test file first to understand the exact current test structure. Then apply these changes:

**1. Update the monkeypatch line (was line ~185):**

BEFORE:
  monkeypatch.setattr("core.prompt_templates.generator_prompt_template", RunnableLambda(lambda x: x))

AFTER:
  monkeypatch.setattr(
      "core.prompt_templates.generator_messages",
      lambda query, references, target_language, chat_history: chat_history,
  )

Explanation: The lambda intercepts the call to generator_messages() in pipeline_langgraph.py and returns chat_history directly. pipeline_langgraph.py then calls chat_model.stream(chat_history), so the mock chat_model receives chat_history as its argument.

**2. Update fake_model_fn (was lines ~176-178):**

BEFORE:
  def fake_model_fn(payload):
      captured["history"] = payload["chat_history"]
      return "Generated answer"

AFTER:
  def fake_model_fn(payload):
      captured["history"] = payload  # payload is now the chat_history list directly
      return "Generated answer"

Explanation: After the refactor, pipeline_langgraph.py calls chat_model.stream(messages) where messages is what generator_messages() returns. The lambda makes generator_messages() return chat_history directly, so fake_model_fn receives that list as payload. The assertion at line ~206 (captured["history"][0].content == "Earlier context") still works because chat_history is [HumanMessage(content="Earlier context")] from FakeHistory.

**3. Check if RunnableLambda is still used elsewhere in the test file.** If not, remove its import to keep the file clean. Read the file to verify before removing any import.

**4. Verify the test assertion logic is still valid:**
The test asserts `captured["history"][0].content == "Earlier context"` — this remains valid because:
- FakeHistory.messages = [HumanMessage(content="Earlier context")]
- pipeline_langgraph.py calls generator_messages(chat_history=history_messages) where history_messages = FakeHistory.messages
- The lambda returns chat_history (= [HumanMessage(content="Earlier context")])
- fake_model_fn receives that list as payload
- captured["history"] = [HumanMessage(content="Earlier context")]
- captured["history"][0].content == "Earlier context" is True
  </action>
  <verify>
    <automated>cd /Users/shawn.n/Desktop/Deen/deen-backend && python -m pytest tests/test_agentic_streaming_pipeline.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'generator_messages' tests/test_agentic_streaming_pipeline.py returns at least 1
    - grep -c 'generator_prompt_template' tests/test_agentic_streaming_pipeline.py returns 0
    - pytest tests/test_agentic_streaming_pipeline.py -q exits with code 0
    - pytest output shows no FAILED lines
  </acceptance_criteria>
  <done>Test file updated: monkeypatch patches generator_messages (not generator_prompt_template); fake_model_fn receives list payload; pytest -q passes for this test file</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 replaced all ChatPromptTemplate.from_messages in core/prompt_templates.py (6 templates) and all 6 modules/fiqh/ files.
    Plan 02 updated 6 consumer files: classifier.py, translator.py, generator.py, stream_generator.py (removed with_redis_history), pipeline_langgraph.py (both fiqh and non-fiqh paths), primer_service.py (both call sites).
    Plan 03 updated the test monkeypatch and pytest passes.

    Automated checks run — now verifying Phase 18 success criteria end to end.
  </what-built>
  <how-to-verify>
Run these checks from the project root:

1. Confirm ChatPromptTemplate is gone from all refactored modules:
   grep -rn "ChatPromptTemplate.from_messages" modules/fiqh/ modules/classification/ modules/translation/ modules/generation/ core/pipeline_langgraph.py
   Expected: 0 results

2. Confirm enhancer templates are untouched in prompt_templates.py:
   grep -c "ChatPromptTemplate.from_messages" core/prompt_templates.py
   Expected: 2

3. Confirm enhancer exclusion comment exists:
   grep -c "NOT refactored to make_cached_system_message" core/prompt_templates.py
   Expected: 1

4. Run the full test suite:
   pytest tests -q
   Expected: all tests pass (no failures)

5. Confirm modules/enhancement/enhancer.py is untouched (Phase 18 success criterion 3):
   git diff modules/enhancement/enhancer.py
   Expected: no output (file unchanged)

6. Optional smoke test — if the server is running locally:
   curl -X POST http://127.0.0.1:8000/chat/agentic -H "Content-Type: application/json" -d '{"user_query":"What is taqwa?","session_id":"test-phase18"}' | head -50
   Expected: valid JSON response with an answer (not a 500 error)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures for diagnosis</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite to production code | Tests mock LLM calls — fake_model_fn must receive the correct message structure or tests silently validate wrong behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | test assertion logic after refactor | mitigate | fake_model_fn receives list not dict; acceptance_criteria verifies pytest passes confirming the assertion still fires correctly |
| T-18-07 | Information Disclosure | modules/enhancement/enhancer.py touched by mistake | mitigate | git diff check in verification confirms file is unchanged (Phase 18 success criterion 3) |
</threat_model>

<verification>
Full Phase 18 verification:

    cd /Users/shawn.n/Desktop/Deen/deen-backend

    # Success criterion 1: no ChatPromptTemplate.from_messages in refactored scope
    grep -rn "ChatPromptTemplate.from_messages" modules/fiqh/ modules/classification/ modules/translation/ modules/generation/ core/pipeline_langgraph.py
    # Expected: 0 results

    # Success criterion 2: all static system prompts use make_cached_system_message
    grep -rn "make_cached_system_message" modules/fiqh/ core/prompt_templates.py
    # Expected: 6 results in modules/fiqh/ + 4 in prompt_templates.py

    # Success criterion 3: enhancer is untouched
    git diff modules/enhancement/enhancer.py
    # Expected: empty output

    # Success criterion 4: pytest passes
    pytest tests -q
    # Expected: all green
</verification>

<success_criteria>
- grep for ChatPromptTemplate.from_messages in modules/fiqh/ returns 0 results
- grep for ChatPromptTemplate.from_messages in core/prompt_templates.py returns exactly 2 (enhancers only)
- grep for NOT refactored to make_cached_system_message in core/prompt_templates.py returns 1
- git diff modules/enhancement/enhancer.py shows no changes
- pytest tests -q exits 0 with no failures
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/18-module-prompt-restructuring/18-03-SUMMARY.md`
</output>
