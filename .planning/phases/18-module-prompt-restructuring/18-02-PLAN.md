---
phase: 18-module-prompt-restructuring
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - modules/classification/classifier.py
  - modules/translation/translator.py
  - modules/generation/stream_generator.py
  - modules/generation/generator.py
  - core/pipeline_langgraph.py
  - services/primer_service.py
autonomous: true
requirements:
  - STRUCT-02

must_haves:
  truths:
    - "modules/classification/classifier.py calls prompt_templates.fiqh_classifier_messages() and nonislamic_classifier_messages()"
    - "modules/translation/translator.py calls prompt_templates.translation_messages()"
    - "modules/generation/stream_generator.py fetches history explicitly and calls chat_model.stream(messages) — no with_redis_history wrapper"
    - "modules/generation/generator.py calls prompt_templates.generator_messages() — no .invoke().to_messages() chain"
    - "core/pipeline_langgraph.py imports _build_messages from modules/fiqh/generator (not _prompt) and uses fiqh_build_messages() in streaming loop"
    - "core/pipeline_langgraph.py non-fiqh generation path calls prompt_templates.generator_messages() and chat_model.stream(messages)"
    - "services/primer_service.py calls primer_generation_messages() in both _generate_bullets_with_llm and _stream_bullets_with_llm"
  artifacts:
    - path: "modules/classification/classifier.py"
      provides: "updated call sites using builder functions"
      contains: "fiqh_classifier_messages"
    - path: "modules/translation/translator.py"
      provides: "updated call site using builder function"
      contains: "translation_messages"
    - path: "modules/generation/stream_generator.py"
      provides: "explicit history fetch replacing with_redis_history"
      contains: "make_history(session_id).messages"
    - path: "core/pipeline_langgraph.py"
      provides: "updated fiqh and non-fiqh generation paths"
      contains: "fiqh_build_messages"
    - path: "services/primer_service.py"
      provides: "updated primer call sites"
      contains: "primer_generation_messages"
  key_links:
    - from: "modules/classification/classifier.py"
      to: "core/prompt_templates.fiqh_classifier_messages"
      via: "prompt_templates.fiqh_classifier_messages(query=query, chatContext=chatContext)"
      pattern: "fiqh_classifier_messages"
    - from: "core/pipeline_langgraph.py"
      to: "modules/fiqh/generator._build_messages"
      via: "from modules.fiqh.generator import (_build_messages as fiqh_build_messages, ...)"
      pattern: "fiqh_build_messages"
    - from: "modules/generation/stream_generator.py"
      to: "core/memory.make_history"
      via: "history_messages = make_history(session_id).messages"
      pattern: "make_history"
---

<objective>
Update all consumer call sites that import ChatPromptTemplate objects from core/prompt_templates.py or reference the _prompt variable from modules/fiqh/generator.py. These 6 files break after Plan 01 renames the exported symbols — this plan updates them to call the new builder functions.

Purpose: Plan 01 replaced ChatPromptTemplate objects with builder functions. Any code that still calls .invoke({}).to_messages() or _prompt.format_messages() or chain = template | model will break at runtime. This plan fixes all such consumers.

Output:
- modules/classification/classifier.py: 2 call sites updated
- modules/translation/translator.py: 1 call site updated
- modules/generation/stream_generator.py: 2 call sites updated, with_redis_history removed
- modules/generation/generator.py: 1 call site updated
- core/pipeline_langgraph.py: 2 call sites updated (fiqh import + non-fiqh generation path)
- services/primer_service.py: import + 2 call sites updated
</objective>

<execution_context>
@/Users/shawn.n/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawn.n/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-module-prompt-restructuring/18-CONTEXT.md
@.planning/phases/18-module-prompt-restructuring/18-RESEARCH.md
@.planning/phases/18-module-prompt-restructuring/18-PATTERNS.md
@.planning/phases/18-module-prompt-restructuring/18-01-SUMMARY.md

<interfaces>
<!-- Builder functions now in core/prompt_templates.py after Plan 01 -->
fiqh_classifier_messages(query: str, chatContext: str) -> list
nonislamic_classifier_messages(query: str, chatContext: str) -> list
translation_messages(source_language: str, text: str) -> list
generator_messages(query, references, target_language="english", chat_history=None) -> list
hikmah_elaboration_messages(selected_text, context_text, hikmah_tree_name, lesson_name, lesson_summary, references) -> list
primer_generation_messages(lesson_title, lesson_content, baseline_bullets, user_learning_notes, user_interest_notes, user_knowledge_notes, user_preference_notes) -> list

<!-- modules/fiqh/generator.py now exports _build_messages (renamed from _prompt) after Plan 01 -->
_build_messages(query: str, evidence: str) -> list

<!-- core/memory.py make_history -->
make_history(session_id: str) -> object with .messages attribute (list[BaseMessage])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update classifier.py, translator.py, generator.py consumer call sites</name>
  <read_first>
    - modules/classification/classifier.py (read entire file — 2 call sites; verify whether check_if_fiqh and check_if_non_islamic function shapes)
    - modules/translation/translator.py (read entire file — 1 call site)
    - modules/generation/generator.py (read entire file — 1 legacy call site; check import style at top)
    - .planning/phases/18-module-prompt-restructuring/18-PATTERNS.md (Pattern 5 section and modules/generation/generator.py section)
  </read_first>
  <files>modules/classification/classifier.py, modules/translation/translator.py, modules/generation/generator.py</files>
  <action>
**modules/classification/classifier.py — update 2 call sites:**

In check_if_fiqh():
  BEFORE: prompt = prompt_templates.fiqh_classifier_system_prompt.invoke({"query": query, "chatContext": chatContext})
          response = chat_model.invoke(prompt.to_messages())
  AFTER:  messages = prompt_templates.fiqh_classifier_messages(query=query, chatContext=chatContext)
          response = chat_model.invoke(messages)

In check_if_non_islamic():
  BEFORE: prompt = prompt_templates.nonislamic_classifer_prompt_template.invoke({"query": query, "chatContext": chatContext})
          response = chat_model.invoke(prompt.to_messages())
  AFTER:  messages = prompt_templates.nonislamic_classifier_messages(query=query, chatContext=chatContext)
          response = chat_model.invoke(messages)

Imports at top of file: from core import prompt_templates — unchanged (still works as module import).

---

**modules/translation/translator.py — update 1 call site:**

  BEFORE: prompt = prompt_templates.translation_prompt_template.invoke({"source_language": source_language or "unknown", "text": text})
          response = chat_model.invoke(prompt.to_messages())
  AFTER:  messages = prompt_templates.translation_messages(source_language=source_language or "unknown", text=text)
          response = chat_model.invoke(messages)

Imports: unchanged.

---

**modules/generation/generator.py — update 1 legacy call site:**

  BEFORE: prompt = prompt_templates.generator_prompt_template.invoke({"query":query,"references":references})
          response = chat_model.invoke(prompt.to_messages())
  AFTER:  messages = prompt_templates.generator_messages(query=query, references=references)
          response = chat_model.invoke(messages)

generator_messages defaults target_language="english" and chat_history=None — calling with only query+references is correct for this legacy path (no chat history, no language override). Per RESEARCH.md Undocumented #2, this is the expected usage.

Import check: read the file top to see whether it uses `from core.prompt_templates import generator_prompt_template` (by name) or `from core import prompt_templates` (module). If by name: change import to `from core.prompt_templates import generator_messages`. If module import: no import change needed.
  </action>
  <verify>
    <automated>grep -rn 'to_messages' /Users/shawn.n/Desktop/Deen/deen-backend/modules/classification/classifier.py /Users/shawn.n/Desktop/Deen/deen-backend/modules/translation/translator.py /Users/shawn.n/Desktop/Deen/deen-backend/modules/generation/generator.py</automated>
  </verify>
  <acceptance_criteria>
    - grep for 'to_messages' in modules/classification/classifier.py returns 0 results
    - grep for 'to_messages' in modules/translation/translator.py returns 0 results
    - grep for 'to_messages' in modules/generation/generator.py returns 0 results
    - grep -c 'fiqh_classifier_messages' modules/classification/classifier.py returns 1
    - grep -c 'nonislamic_classifier_messages' modules/classification/classifier.py returns 1
    - grep -c 'translation_messages' modules/translation/translator.py returns 1
    - grep -c 'generator_messages' modules/generation/generator.py returns 1
    - grep -c 'fiqh_classifier_system_prompt' modules/classification/classifier.py returns 0
    - grep -c 'nonislamic_classifer_prompt_template' modules/classification/classifier.py returns 0
    - grep -c 'translation_prompt_template' modules/translation/translator.py returns 0
    - grep -c 'generator_prompt_template' modules/generation/generator.py returns 0
  </acceptance_criteria>
  <done>3 consumer files updated: classifier.py has 2 call sites using builder functions, translator.py has 1 call site, generator.py has 1 call site. No .invoke({}).to_messages() pattern remains in any of these 3 files.</done>
</task>

<task type="auto">
  <name>Task 2: Update stream_generator.py, pipeline_langgraph.py, primer_service.py</name>
  <read_first>
    - modules/generation/stream_generator.py (read entire file — 2 generator functions; check current import of with_redis_history at line 5)
    - core/pipeline_langgraph.py (read lines 220-230 for fiqh import block; read lines 325-355 for non-fiqh generation path)
    - services/primer_service.py (read import at line 17; read lines 785-800 for _generate_bullets_with_llm; read lines 850-875 for _stream_bullets_with_llm)
    - .planning/phases/18-module-prompt-restructuring/18-PATTERNS.md (Pattern 6 for stream_generator, Pattern 7, Pattern 8, Pattern 9, Pattern 10)
  </read_first>
  <files>modules/generation/stream_generator.py, core/pipeline_langgraph.py, services/primer_service.py</files>
  <action>
**modules/generation/stream_generator.py — 2 changes:**

1. Fix import at top of file (line 5):
   BEFORE: from core.memory import with_redis_history, trim_history, make_history
   AFTER:  from core.memory import trim_history, make_history
   Rationale: with_redis_history is no longer used in this file after this refactor. Per RESEARCH.md Pitfall 5: leaving it imported would be an unused import.

2. Update generate_response_stream() — replace with_redis_history chain with explicit history fetch:
   BEFORE:
     prompt = prompt_templates.generator_prompt_template
     chain = prompt | chat_model
     chain_with_history = with_redis_history(chain)
     for chunk in chain_with_history.stream(
         {"target_language": target_language, "query": query, "references": references},
         config={"configurable": {"session_id": session_id}},
     ):
         yield getattr(chunk, "content", str(chunk) if chunk is not None else "")

   AFTER:
     history_messages = make_history(session_id).messages
     messages = prompt_templates.generator_messages(
         query=query,
         references=references,
         target_language=target_language,
         chat_history=history_messages,
     )
     for chunk in chat_model.stream(messages):
         yield getattr(chunk, "content", str(chunk) if chunk is not None else "")

3. Update generate_elaboration_response_stream() — remove chain pattern:
   BEFORE:
     prompt = prompt_templates.hikmah_elaboration_prompt_template
     chain = prompt | chat_model
     for chunk in chain.stream(
         {"selected_text": selected_text, "context_text": context_text,
          "hikmah_tree_name": hikmah_tree_name, "lesson_name": lesson_name,
          "lesson_summary": lesson_summary, "references": references}):
         content = getattr(chunk, "content", str(chunk) if chunk is not None else "")

   AFTER:
     messages = prompt_templates.hikmah_elaboration_messages(
         selected_text=selected_text,
         context_text=context_text,
         hikmah_tree_name=hikmah_tree_name,
         lesson_name=lesson_name,
         lesson_summary=lesson_summary,
         references=references,
     )
     for chunk in chat_model.stream(messages):
         content = getattr(chunk, "content", str(chunk) if chunk is not None else "")

   Note: `prompt | chat_model` raises TypeError when prompt is a plain Python function (not a LangChain Runnable). After Plan 01, hikmah_elaboration_prompt_template is now a function. The chain pattern must be replaced. Per RESEARCH.md Pitfall 4.

---

**core/pipeline_langgraph.py — 2 change sites:**

Change site 1: fiqh path import (around line 224-225):
   BEFORE:
     from modules.fiqh.generator import (
         _prompt as fiqh_prompt,
         _format_evidence,
         _build_references_section,
         INSUFFICIENT_WARNING,
         FATWA_DISCLAIMER,
     )
   AFTER:
     from modules.fiqh.generator import (
         _build_messages as fiqh_build_messages,
         _format_evidence,
         _build_references_section,
         INSUFFICIENT_WARNING,
         FATWA_DISCLAIMER,
     )

Change site 1 continued: fiqh streaming loop (around lines 267-273):
   BEFORE:
     model = chat_models.get_generator_model()
     chain = fiqh_prompt | model
     response_tokens = []
     for chunk in chain.stream({
         "query": user_query,
         "evidence": _format_evidence(fiqh_docs),
     }):
   AFTER:
     model = chat_models.get_generator_model()
     fiqh_messages = fiqh_build_messages(
         query=user_query,
         evidence=_format_evidence(fiqh_docs),
     )
     response_tokens = []
     for chunk in model.stream(fiqh_messages):

Change site 2: non-fiqh generation path (around lines 332-344):
   BEFORE:
     prompt = prompt_templates.generator_prompt_template
     chain = prompt | chat_model
     history_messages = make_history(runtime_session_id).messages
     response_tokens = []
     for chunk in chain.stream(
         {
             "target_language": target_language,
             "query": user_query,
             "references": references,
             "chat_history": history_messages,
         },
     ):
   AFTER:
     history_messages = make_history(runtime_session_id).messages
     messages = prompt_templates.generator_messages(
         query=user_query,
         references=references,
         target_language=target_language,
         chat_history=history_messages,
     )
     response_tokens = []
     for chunk in chat_model.stream(messages):

Read the actual line numbers before making changes — the lines above are approximate. The exact code content matters more than the line number.

---

**services/primer_service.py — import + 2 call sites:**

Import at line ~17:
   BEFORE: from core.prompt_templates import primer_generation_prompt_template
   AFTER:  from core.prompt_templates import primer_generation_messages

Call site 1 — _generate_bullets_with_llm method (around line 792):
   BEFORE:
     formatted_prompt = primer_generation_prompt_template.invoke(prompt_inputs)
     response = await primers_model.ainvoke(formatted_prompt)
   AFTER:
     messages = primer_generation_messages(**prompt_inputs)
     response = await primers_model.ainvoke(messages)

Call site 2 — _stream_bullets_with_llm method (around lines 859+):
   BEFORE:
     formatted_prompt = primer_generation_prompt_template.invoke(prompt_inputs)
     ...
     async for chunk in primers_model.astream(formatted_prompt):
   AFTER:
     messages = primer_generation_messages(**prompt_inputs)
     ...
     async for chunk in primers_model.astream(messages):

prompt_inputs is a dict with keys: lesson_title, lesson_content, baseline_bullets, user_learning_notes, user_interest_notes, user_knowledge_notes, user_preference_notes — these match primer_generation_messages parameter names exactly. The **prompt_inputs unpacking is correct.

IMPORTANT: Both call sites in primer_service.py must be updated. Read the file to find both occurrences before editing. Per RESEARCH.md Pitfall 7.
  </action>
  <verify>
    <automated>grep -rn 'with_redis_history\|fiqh_prompt\|generator_prompt_template\|hikmah_elaboration_prompt_template\|primer_generation_prompt_template' /Users/shawn.n/Desktop/Deen/deen-backend/modules/generation/stream_generator.py /Users/shawn.n/Desktop/Deen/deen-backend/core/pipeline_langgraph.py /Users/shawn.n/Desktop/Deen/deen-backend/services/primer_service.py</automated>
  </verify>
  <acceptance_criteria>
    - grep for 'with_redis_history' in modules/generation/stream_generator.py returns 0 results
    - grep for 'chain = fiqh_prompt' in core/pipeline_langgraph.py returns 0 results
    - grep for '_prompt as fiqh_prompt' in core/pipeline_langgraph.py returns 0 results
    - grep -c 'fiqh_build_messages' core/pipeline_langgraph.py returns at least 1
    - grep for 'primer_generation_prompt_template' in services/primer_service.py returns 0 results
    - grep -c 'primer_generation_messages' services/primer_service.py returns 2 (both call sites)
    - grep -c 'hikmah_elaboration_messages' modules/generation/stream_generator.py returns 1
    - grep -c 'generator_messages' modules/generation/stream_generator.py returns 1
    - grep for 'hikmah_elaboration_prompt_template' in modules/generation/stream_generator.py returns 0 results
    - grep for 'generator_prompt_template' in modules/generation/stream_generator.py returns 0 results
    - grep for 'generator_prompt_template' in core/pipeline_langgraph.py returns 0 results
  </acceptance_criteria>
  <done>4 consumer files updated: stream_generator.py uses explicit history + direct model.stream(); pipeline_langgraph.py imports fiqh_build_messages and uses it in fiqh streaming loop + uses generator_messages in non-fiqh path; primer_service.py uses primer_generation_messages in both LLM call methods. No chain = template | model pattern remains in any updated file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module code to Anthropic API | Prompt messages constructed here are sent verbatim to API |
| Redis history fetch | make_history(session_id).messages fetches untrimmed history; trim_history already called in stream_generator.py — no change to trim logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | pipeline_langgraph.py fiqh import | mitigate | Import _build_messages (not _prompt) — wrong import would cause silent wrong behavior or ImportError at fiqh query time |
| T-18-04 | Information Disclosure | stream_generator.py history fetch | accept | make_history(session_id).messages is the same history source previously used by with_redis_history; no behavioral change |
| T-18-05 | Denial of Service | primer_service.py only one call site updated | mitigate | Both lines 792 and 859 must be updated; RESEARCH.md Pitfall 7 documents the risk; acceptance_criteria checks count=2 |
</threat_model>

<verification>
After both tasks complete:

    cd /Users/shawn.n/Desktop/Deen/deen-backend

    # No old-style ChatPromptTemplate call sites remain in consumers
    grep -rn 'to_messages' modules/classification/ modules/translation/ modules/generation/ services/primer_service.py
    # Expected: 0 results

    # No old template object names remain
    grep -rn 'fiqh_classifier_system_prompt\|nonislamic_classifer_prompt_template\|translation_prompt_template\|generator_prompt_template\|hikmah_elaboration_prompt_template\|primer_generation_prompt_template' modules/ services/ core/pipeline_langgraph.py
    # Expected: 0 results

    # fiqh_build_messages used in pipeline
    grep -c 'fiqh_build_messages' core/pipeline_langgraph.py
    # Expected: 2 (import alias + call site)

    # Python import smoke test
    python3 -c "import sys; sys.path.insert(0, '.'); from modules.classification import classifier; print('classifier import OK')"
    # Expected: "classifier import OK"
</verification>

<success_criteria>
- grep for 'to_messages' in all 6 modified files returns 0 results
- grep for '_prompt as fiqh_prompt' in core/pipeline_langgraph.py returns 0 results
- grep for 'with_redis_history' in stream_generator.py returns 0 results
- grep -c 'primer_generation_messages' services/primer_service.py returns 2
- grep -c 'fiqh_build_messages' core/pipeline_langgraph.py returns at least 2 (import + call)
- Python imports succeed for all 6 modified modules
</success_criteria>

<output>
After completion, create `.planning/phases/18-module-prompt-restructuring/18-02-SUMMARY.md`
</output>
